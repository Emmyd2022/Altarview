// Single source of truth for the app's name and identity strings.
// Change here, not per-screen — see ALT-001 in the project backlog.

export const APP_NAME = 'Altarview'
export const APP_INITIALS = 'AV'
export const APP_OWNER = 'RCCG CAYC Media Team'
export const PLUGIN_DIR_HINT = '~/altarview/plugins/'
